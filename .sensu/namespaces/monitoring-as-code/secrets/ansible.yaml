---
type: Secret
api_version: secrets/v1
metadata:
  name: ansible_tower_token
spec:
  id: ANSIBLE_TOKEN
  provider: env
---
type: Secret
api_version: secrets/v1
metadata:
  name: ansible_tower_host
spec:
  id: ANSIBLE_HOST
  provider: env
